import { VetNameSearchPipe } from './vet-name-search.pipe';

describe('VetNameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new VetNameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
