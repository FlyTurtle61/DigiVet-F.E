import { AdminGetallUserPipe } from './admin-getall-user.pipe';

describe('AdminGetallUserPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminGetallUserPipe();
    expect(pipe).toBeTruthy();
  });
});
