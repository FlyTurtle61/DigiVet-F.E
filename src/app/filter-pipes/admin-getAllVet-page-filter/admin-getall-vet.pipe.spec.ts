import { AdminGetallVetPipe } from './admin-getall-vet.pipe';

describe('AdminGetallVetPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminGetallVetPipe();
    expect(pipe).toBeTruthy();
  });
});
