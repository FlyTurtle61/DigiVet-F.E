import { MeetingControlPipe } from './meeting-control.pipe';

describe('MeetingControlPipe', () => {
  it('create an instance', () => {
    const pipe = new MeetingControlPipe();
    expect(pipe).toBeTruthy();
  });
});
