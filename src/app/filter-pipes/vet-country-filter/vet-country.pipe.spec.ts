import { VetCountryPipe } from './vet-country.pipe';

describe('VetCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new VetCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
