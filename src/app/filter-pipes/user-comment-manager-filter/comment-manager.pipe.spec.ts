import { CommentManagerPipe } from './comment-manager.pipe';

describe('CommentManagerPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentManagerPipe();
    expect(pipe).toBeTruthy();
  });
});
