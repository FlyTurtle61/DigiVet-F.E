import { AdminCancelledPipe } from './admin-cancelled.pipe';

describe('AdminCancelledPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminCancelledPipe();
    expect(pipe).toBeTruthy();
  });
});
